[package]
name = "redis_cli_client"
version = "0.1.0"
edition = "2024"

[dependencies]
deadpool-redis = "0.23.0"
redis = { version = "1.1.0", features = ["tokio-comp"] }
tokio = { version = "1", features = ["full"] }
